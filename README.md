Running this server is pretty simple make sure you use a valid html file and initialize it to file pointer which is being read.
compile the code by running - gcc -o compile exe_file_name server.c 
and then run the file with ./exe_file_name
